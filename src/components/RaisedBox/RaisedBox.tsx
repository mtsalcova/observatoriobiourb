import styled from 'styled-components';

import { Wrapper } from 'components';

export const RaisedBox = styled(Wrapper)`
  background: white;
  border-radius: 4px;
  margin-top: -50px;
  padding: 84px 0;
`;
